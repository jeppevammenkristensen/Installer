<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="SimpleInstaller.MainWindow"
        Title="MainWindow" Height="350" Width="525">
	<Window.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
	</Window.Resources>
	<Grid d:DataContext="{d:DesignData /SampleData/InstallationViewModelSampleData.xaml}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="150*"/>
			<RowDefinition Height="7*"/>
			<RowDefinition Height="142*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<ListBox Grid.Column="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch" Visibility="{Binding IsValid, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.Row="1" Margin="5" >
			<GridSplitter Height="5.98" Width="39"/>
		</ListBox>

		<Button Content="Install" Grid.Row="4" Click="Install_Clicked" />
		<GridSplitter HorizontalAlignment="Stretch" Margin="1" Grid.Row="2"/>
		<ListBox Margin="5" Grid.Row="3"/>
        
	</Grid>
</Window>
